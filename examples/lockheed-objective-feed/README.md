# Synthetic Lockheed objective feed fixtures

These files are synthetic test data created for the A2O Technical Baseline
Manager. They are not Lockheed Martin, Government, GitLab, Jira, JPO, or
program records. Do not load them into a workspace containing controlled data.

Upload `01-synthetic-day-one.json` and apply it. Then upload
`02-synthetic-day-two.json` and inspect the preview. The second file
demonstrates an added subject (`14`), changed subject (`9`), removed subject
(`13`), unchanged/no-JPO subject (`arch_plan_44`), multi-JPO values (`9`), and
resolved plus unresolved dependency references.
